/**
   Use the decrement operator(--) to write a while that prints the numbers from ten down to zero.
 */
#include <iostream>
using namespace std;

int main() {
	int num = 11;
	while(num--)
		cout << num << endl;

	return 0;
} 
